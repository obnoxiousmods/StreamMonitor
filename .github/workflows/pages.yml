name: Deploy Docs to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'README.md'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare docs source
        run: |
          mkdir -p _docs/screenshots

          # Copy README as index
          cp README.md _docs/index.md

          # Copy all docs
          cp docs/*.md _docs/

          # Copy screenshots (referenced by README)
          cp -r docs/screenshots/* _docs/screenshots/ 2>/dev/null || true

          # Fix image paths in index.md (docs/screenshots/ -> screenshots/)
          sed -i 's|docs/screenshots/|screenshots/|g' _docs/index.md

          # Create Jekyll config
          cat > _docs/_config.yml << 'YAML'
          title: StreamMonitor
          description: Real-time infrastructure monitoring for self-hosted media streaming stacks
          theme: jekyll-theme-midnight
          markdown: GFM
          YAML

      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: _docs
          destination: _site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
